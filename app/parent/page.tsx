'use client';
import Image from 'next/image';
import Link from 'next/link';
import { useEffect, useState } from 'react';
import { supabase } from '../../lib/supabase';

type Detail={student_id:string;parent_id:string;student_name:string;admission_number:string|null;portal_code:string|null;parent_name:string|null;parent_phone:string|null};
export default function ParentPortal(){const[rows,setRows]=useState<Detail[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');
 useEffect(()=>{async function load(){const token=sessionStorage.getItem('aic_parent_portal_token');if(!token){window.location.href='/parent-login';return}if(!supabase){setError('Parent portal is not configured.');setLoading(false);return}const{data,error}=await supabase.rpc('parent_portal_session_details',{p_token:token});if(error||!data?.length){sessionStorage.removeItem('aic_parent_portal_token');setError('Your portal session has expired. Please log in again.');setLoading(false);return}setRows(data);setLoading(false)}load()},[]);
 function signOut(){sessionStorage.removeItem('aic_parent_portal_token');window.location.href='/parent-login'}
 if(loading)return <main className="placeholder"><div className="box"><div className="eyebrow">PARENT PORTAL</div><h1>Loading your account…</h1><p>Securely retrieving your parent and learner information.</p></div></main>;
 if(error)return <main className="placeholder"><div className="box"><div className="eyebrow">PARENT PORTAL</div><h1>Unable to open portal</h1><div className="error">{error}</div><Link href="/parent-login" className="portal-submit">LOGIN AGAIN</Link></div></main>;
 const first=rows[0];return <main className="portal-dashboard"><div className="portal-header"><Link href="/"><Image src="/aic-cathedral-logo.svg" width={62} height={62} alt="AIC Cathedral logo"/></Link><div><div className="eyebrow">PARENT PORTAL</div><h1>Welcome{first?.parent_name?`, ${first.parent_name}`:''}</h1></div><button className="logout" onClick={signOut}>LOG OUT</button></div><section className="portal-info-grid"><article className="portal-info-card"><span>Parent Phone Number</span><strong>{first?.parent_phone||'Not provided'}</strong></article></section><section className="portal-students"><div className="heading"><span>LINKED LEARNERS</span><h2>Student Special Codes</h2><p>These permanent codes are retrieved directly from the school's student records.</p></div><div className="student-cards">{rows.map(s=><article className="student-card" key={s.student_id}><div className="student-card-top"><div><span className="student-label">LEARNER</span><h3>{s.student_name}</h3></div><div className="special-code"><span>STUDENT SPECIAL CODE</span><strong>{s.portal_code||'Not assigned'}</strong></div></div>{s.admission_number&&<p>Admission Number: <strong>{s.admission_number}</strong></p>}</article>)}</div></section></main>}
